apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: todo-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/asfalfa/todo-app-config.git
    targetRevision: main
    path: apps/staging
  destination:
    name: in-cluster
    namespace: todo-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
